import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "TapMenu - Şifre Oluştur",
};

export default function SetPasswordLayout({ children }: { children: React.ReactNode }) {
  return children;
}
